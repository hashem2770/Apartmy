import 'package:flutter/material.dart';
class AddingBlockViewBody extends StatelessWidget {
  const AddingBlockViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
